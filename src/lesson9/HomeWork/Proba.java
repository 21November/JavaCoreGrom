package lesson9.HomeWork;

public class Proba {
    /*
    public User save(User user){
        for(User usr: users) {
            if(usr != null) {
                return null;
            }
        }

        for (int index = 0; index < users.length; index++) {
            if (user == findById(index)) {
                users[index] = null;

            }
            else users[index] = user;
        }
        return user;

    }
    */
}
